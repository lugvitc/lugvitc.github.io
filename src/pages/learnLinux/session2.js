import CodeBlock from '../../components/codeBlock/codeBlock';

// know the terminal
export default function Session2() {
    return (
        <>
            type cd to switch directories:
            <CodeBlock>cd</CodeBlock>
            <CodeBlock>ls</CodeBlock>
        </>
    );
}
